import { EncodingRSocket } from "../encoding-rsocket-client";
export declare namespace TextCoding {
    const Plain: {
        encoder: (data: string, rsocket: EncodingRSocket) => Uint8Array;
        decoder: (buffer: Uint8Array, rsocket: EncodingRSocket) => string;
    };
}
