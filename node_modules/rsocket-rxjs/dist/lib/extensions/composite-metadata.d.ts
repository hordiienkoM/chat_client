import { Authentication } from "./security/authentication";
export interface DecodedMetadata {
    mimeType: string;
    data: any;
}
export declare class CompositeMetadata implements Iterable<DecodedMetadata> {
    private readonly _array;
    get length(): number;
    push(data: DecodedMetadata): void;
    get(index: number): DecodedMetadata | undefined;
    remove(index: number): DecodedMetadata | undefined;
    pop(): DecodedMetadata | undefined;
    [Symbol.iterator](): Iterator<DecodedMetadata, any, undefined>;
    get route(): string | undefined;
    set route(value: string | undefined);
    get authentication(): Authentication | undefined;
    set authentication(auth: Authentication | undefined);
    getByMimeType<T = any>(mimeType: string): T | undefined;
}
