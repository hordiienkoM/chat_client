/// <reference types="node" />
export declare class RoutingMetadata implements Iterable<string> {
    _buffer: Buffer;
    constructor(buffer: Buffer);
    iterator(): Iterator<string>;
    [Symbol.iterator](): Iterator<string>;
}
/**
 * Encode given set of routes into {@link Buffer} following the <a href="https://github.com/rsocket/rsocket/blob/master/Extensions/Routing.md">Routing Metadata Layout</a>
 *
 * @param routes non-empty set of routes
 * @returns {Buffer} with encoded content
 */
export declare function encodeRoutes(...routes: string[]): Buffer;
export declare function encodeRoute(route: string): Buffer;
export declare function decodeRoutes(routeMetadataBuffer: Buffer): Generator<string, void, any>;
